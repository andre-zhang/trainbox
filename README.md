# Trainbox

Transit map editor. This folder is **self-contained**: upload or clone **only `trainbox/`** to run the full app (configs, `src/`, `public/`, optional `scripts/`).

## Run

```bash
npm install
npm run dev
```

Open: http://localhost:5173

## Build

```bash
npm run build
```
